<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Horizontal Scroll Example</title>
    <style>
        /* Basic reset and styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .container {
            text-align: center;
            z-index: 1;
            background-color: rgba(255, 255, 255, 0.1);
            /* Semi-transparent white background */
            backdrop-filter: blur(10px);
            /* Blur effect for glassmorphism */
            border-radius: 12px;
            /* Rounded corners */
            padding: 40px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            font-family: cursive;
            font-size: medium;
            /* Soft shadow */
        }

        .container:hover {
            background-color: rgba(255, 255, 255, 0.2);
            /* Lighter semi-transparent white on hover */
            backdrop-filter: blur(25px);
            font-size: large;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 20px;
        }

        p {
            font-size: 1.2rem;
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .rightdiv {
            overflow-x: auto;
            /* Enable horizontal scrolling */
            display: flex;
            /* Use flexbox for horizontal alignment */
            justify-content: flex-start;
            /* Start align items */
            z-index: 1;
            flex-direction: row;
            align-items: center;
            background-color: rgba(34, 139, 174, 0.1);
            /* Semi-transparent white background */
            backdrop-filter: blur(10px);
            /* Blur effect for glassmorphism */
            border-radius: 12px;
            /* Rounded corners */
            padding: 40px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            white-space: nowrap;
            /* Prevent wrapping of items */
        }

        .rightdiv .container {
            flex: 0 0 auto;
            /* Prevent containers from growing */
            min-width: 300px;
            /* Set a minimum width for containers */
            margin-right: 20px;
            /* Add space between containers */
        }

        .rightdiv .container:last-child {
            margin-right: 0;
            /* Remove margin from the last container */
        }

        .middiv {
            overflow-y: auto;
            /* Enable vertical scrolling */
            z-index: 1;
            background-color: rgba(34, 139, 174, 0.1);
            /* Semi-transparent white background */
            backdrop-filter: blur(10px);
            /* Blur effect for glassmorphism */
            border-radius: 12px;
            /* Rounded corners */
            padding: 40px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .middiv::-webkit-scrollbar,
        .rightdiv::-webkit-scrollbar {
            width: 10px;
            /* width of the scrollbar */
            height: 3px;
            /* height of the scrollbar */
        }

        /* Thumb */
        .middiv::-webkit-scrollbar-thumb,
        .rightdiv::-webkit-scrollbar-thumb {
            background-color: #fafafa;
            /* color of the scroll thumb */
            border-radius: 5px;
            /* roundness of the scroll thumb */
            height: 20px;
            /* height of the thumb */
        }

        /* Track */
        .middiv::-webkit-scrollbar-track,
        .rightdiv::-webkit-scrollbar-track {
            background-color: transparent;
            /* color of the track */
        }

        .blurred {
            filter: blur(20px);
        }

        .bg {
            overflow-y: auto;
            background-image: url('bg.jpg');
            background-size: cover;
            background-position: center;
            font-family: Arial, sans-serif;
            background-color: #272727;
            /* Dark background */
            color: #fff;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
    </style>
</head>

<body class="bg">
    <div class="middiv">
        <div class="container">
            <h1>New Friend</h1>
            <p>AI models help humans by enhancing our abilities and expanding what we can achieve. They transform
                how we work, learn, and solve problems.</p>
        </div>
        <div class="container">
            <h1>Rust vs Java vs Haskell</h1>
            <p>Comparing performance, safety, and functional programming strengths.</p>
        </div>
        <div class="container">
            <a href="./side.html" style="color: white;">
                <h1>HackThon code example</h1>
            </a>
        </div>
    </div>
    <div class="rightdiv">
        <div class="container">
            <p>"Continue to breathe; the tide will carry you to new shores. Stay resilient; do not surrender." (0_1)</p>
        </div>
        <div class="container">
            <h1>Game Plan</h1>
            <p>I have to create a gene 🧬 game 🤔</p>
        </div>
        <div class="container">
            <p>Don't complicate the scenario, make it as simple as possible.</p>
        </div>
        <div class="container">
            <h1 id="mouse-position"></h1>
        </div>
    </div>
    <script src="mousePosition.js"></script>
    <script>
        // Get screen width and height
        const screenWidth = window.screen.width;
        const screenHeight = window.screen.height;

        // Select the div element
        const myDiv = document.getElementById('bg');

        // Set div size based on screen width and height
        myDiv.style.width = `${screenWidth}px`;
        myDiv.style.height = `${screenHeight}px`;

        const bg = document.getElementById('bg');

        // Calculate background size based on screen dimensions
        const backgroundSize = `${screenWidth}px ${screenHeight}px`;

        // Set background-size property
        bg.style.backgroundSize = backgroundSize;
    </script>
</body>

</html>